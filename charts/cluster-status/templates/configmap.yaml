apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "cluster-status.fullname" . }}-app
data:
  main.py: |
{{ .Files.Get "files/main.py" | indent 4 }}

  index.html: |
{{ .Files.Get "files/index.html" | indent 4 }}