apiVersion: v1
kind: ServiceAccount
metadata:
  name: gpu-preflight-sa
  namespace: {{ .Release.Namespace }}

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: gpu-preflight-role
rules:
- apiGroups: [""]
  resources: ["nodes"]
  verbs: ["get", "patch"]

---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: gpu-preflight-binding
subjects:
- kind: ServiceAccount
  name: gpu-preflight-sa
  namespace: {{ .Release.Namespace }}
roleRef:
  kind: ClusterRole
  name: gpu-preflight-role
  apiGroup: rbac.authorization.k8s.io